{
  "name": "monorepo-playground",
  "private": true,
  "scripts": {
    "dev": "pnpm build && concurrently -n services,app -c magenta,green \"pnpm --filter @repo/services dev\" \"pnpm --filter app dev\"",
    "start": "pnpm -r build && pnpm --filter app start",
    "build": "pnpm -r build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
